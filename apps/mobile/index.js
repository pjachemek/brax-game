// Expo entry. `registerRootComponent` calls AppRegistry.registerComponent and
// sets up the root view for both the dev client and a standalone build.
import { registerRootComponent } from 'expo';

import App from './App.tsx';

registerRootComponent(App);
